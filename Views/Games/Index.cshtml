@model IEnumerable<FavGames.Models.Game>

@{
    ViewData["Title"] = "Admin";
}

<h1>Admin</h1>

<p>
    <button style="background-color: #4CAF50; color: white;" onclick="location.href='@Url.Action("Create")'">Create New</button>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Platforms)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Platforms)
            </td>
                <td>
                    <button style="background-color: #4CAF50; color: white;" onclick="location.href='@Url.Action("Edit", new { id = item.Id })'">Edit</button> |
                    <button style="background-color: #4CAF50; color: white;" onclick="location.href='@Url.Action("Details", new { id = item.Id })'">Details</button> |
                    <button style="background-color: #4CAF50; color: white;" onclick="location.href='@Url.Action("Delete", new { id = item.Id })'">Delete</button>
                </td>
           

        </tr>
}
    </tbody>
</table>
        <div style="text-align: right;">
            <button style="background-color: #4CAF50; color: white;" onclick="location.href='@Url.Action("Index", "Home")'">Back to List</button>
        </div>

